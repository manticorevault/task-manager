<nav>

    <div class="nav-wrapper">
        <ul class="left">
            <% if ( auth.isAuthenticated ) { %>
            <li><a class="btn waves-effect waves-yellow" href="/logout">Logout</a></li>
            <% } else { %>
            <li><a class="btn waves-effect waves-yellow" href="/login">Login</a></li>
            <% } %>
        </ul>
    </div>

 </nav>